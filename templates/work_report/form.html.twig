{% extends 'base.html.twig' %}

{% block title %}Envoie fiche d'heures - TimeBoard{% endblock %}

{% block body %}
    <div class="container py-4">
        <turbo-frame id="work-report-form-frame">
            {{ form_start(form, {
                action: path('app_work_report_submit', {'id': workMonth.id}),
            }) }}

            <div class="modal-header">
                <h5 class="modal-title" id="work-report-modal-label">
                    <i class="bi bi-send"></i>
                    Envoyer la fiche d'heures
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body">
                {% set monthDate = (workMonth.year ~ '-' ~ '%02d'|format(workMonth.month) ~ '-01')|date('F') %}
                <p class="text-muted small">Vous êtes sur le point de soumettre la fiche du mois de <strong>{{ monthDate|trans|capitalize }} {{ workMonth.year }}</strong>. Cette action est définitive.</p>

                {{ form_errors(form) }}

                <div class="mb-3">
                    {{ form_label(form.recipientEmail) }}
                    {{ form_widget(form.recipientEmail, { 'attr': { 'class': 'form-control' } }) }}
                    {{ form_errors(form.recipientEmail) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.attachmentPath, 'Pièce jointe (Optionnel)') }}
                    {{ form_widget(form.attachmentPath, {
                        'attr': {
                            'accept': '.pdf,image/*',
                            'data-max-size': 2097152,
                            'class': 'form-control'
                        }
                    }) }}
                    <small class="form-text text-muted">Taille max : 2 Mo.</small>
                    {{ form_errors(form.attachmentPath) }}
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                {{ form_widget(form.submit, {'label': 'Confirmer et Envoyer', 'attr': {'class': 'btn btn-success'}}) }}
            </div>

            {{ form_end(form) }}
        </turbo-frame>


        {#<turbo-frame id="work-report-form-frame">
            <div class="card mb-4 shadow-sm">
                <div class="card-header bg-success text-white text-center">
                    <h5 class="mb-0">
                        <i class="bi bi-send"></i>
                        Envoyer la fiche d'heures
                    </h5>
                </div>
                <div class="card-body">
                    <div data-controller="work-report">
                        {{ form_start(form, {
                            action: path('app_work_report_submit', {'id': workMonth.id}),
                        }) }}

                        {{ form_errors(form) }}

                        <div class="mb-3">
                            {{ form_label(form.recipientEmail) }}
                            {{ form_widget(form.recipientEmail, { 'attr': { 'class': 'form-control' } }) }}
                            {{ form_errors(form.recipientEmail) }}
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.attachmentPath) }}

                            <div class="border rounded p-4 text-center d-none d-md-block flex-grow-1"
                                 style="cursor:pointer;"
                                 onclick="document.getElementById('attachmentPath').click()">
                                <p class="text-muted mb-0">Glissez-déposez votre fichier ici</p>
                            </div>

                            {{ form_widget(form.attachmentPath, {
                                'attr': {
                                    accept: '.pdf,image/*',
                                    'data-max-size': 2097152,
                                    'data-work-report-target': 'fileInput',
                                    'data-action': 'change->work-report#validateFile',
                                    'class': 'form-control d-block d-md-none'
                                }
                            }) }}
                            <small class="form-text text-muted">Taille max : 2 Mo. Formats acceptés : PDF, JPG, PNG, GIF.</small>
                            {{ form_errors(form.attachmentPath) }}
                        </div>

                        <div data-work-report-target="error" class="alert alert-danger mt-2" style="display: none"></div>

                        <div class="d-flex justify-content-end gap-2">
                            <a href="#" class="btn btn-secondary btn-sm" data-action="click->form-toggle#clearFrame">Annuler</a>
                            {{ form_widget(form.submit, {'attr': {'class': 'btn btn-success btn-sm'}}) }}
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </turbo-frame>#}
    </div>
{% endblock %}
