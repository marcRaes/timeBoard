{% extends 'base.html.twig' %}

{% block title %}Hello WorkReportController!{% endblock %}

{% block body %}
    <div class="card mb-4 p-3 border rounded">
        <h2>Envoyer la fiche d'heures</h2>

        {{ form_start(form) }}
            {{ form_row(form.recipientEmail) }}
            {{ form_row(form.attachmentPath) }}
            <button class="btn btn-primary">{{ form_label(form.submit) }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
