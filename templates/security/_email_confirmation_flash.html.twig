<i class="bi bi-exclamation-triangle-fill flex-shrink-0 me-2" style="font-size: 1.25rem;"></i>
<span class="fw-semibold">Votre adresse email n’est pas encore confirmée.</span>
<form class="d-inline" method="post" action="{{ path('app_resend_confirmation') }}">
    <input type="hidden" name="email" value="{{ email }}">
    <button type="submit" class="btn btn-sm btn-outline-primary ms-2">
        Renvoyer le mail de confirmation
    </button>
</form>
